"use client";

import React from "react";

const Header: React.FC = () => (
  <header>
    <h1 className="header font-montserrat">As lentes que refletem você</h1>
  </header>
);

export default Header;
